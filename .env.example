# Anthropic API Configuration (REQUIRED - Choose ONE method)
#
# METHOD 1: Vertex AI (Google Cloud) - Recommended for enterprise
# Use this if your company uses Claude via Google Cloud Vertex AI
# Requires: gcloud auth application-default login
# ANTHROPIC_VERTEX_PROJECT_ID=your-gcp-project-id
# ANTHROPIC_VERTEX_REGION=us-east5  # Optional - SDK auto-selects region if not specified
#
# METHOD 2: Direct API Key - For personal use or free tier
# Get your API key from: https://console.anthropic.com/
# Free tier available ($5 credit for new accounts)
ANTHROPIC_API_KEY=your_api_key_here
#
# Model configuration (applies to both methods)
# Note: Use @ format for Vertex AI model names (e.g., claude-sonnet-4-5@20250929)
BOSS_MODEL=claude-sonnet-4-5@20250929

# DCI MCP Server Configuration
# The dci-mcp-server provides tools for DCI, Jira, and Google Docs

# Use local development version (optional - comment out to use PyPI version)
# If set, credentials will be loaded from the dci-mcp-server's own .env file
# DCI_MCP_SERVER_PATH=/path/to/dci-mcp-server

# Credentials below are ONLY needed when using the PyPI version (DCI_MCP_SERVER_PATH not set)
# When using local development version, configure credentials in dci-mcp-server/.env instead

# DCI API Credentials (for PyPI version only)
# DCI_CLIENT_ID=your_dci_client_id
# DCI_API_SECRET=your_dci_api_secret
# DCI_CS_URL=https://api.distributed-ci.io

# Jira API Credentials (for PyPI version only)
# JIRA_API_TOKEN=your_jira_token
# JIRA_URL=https://issues.redhat.com

# Google Docs (for PyPI version only)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/credentials.json

# Monitoring Configuration (used by ai-assist /monitor)
JIRA_CHECK_INTERVAL=300
DCI_CHECK_INTERVAL=300
JIRA_PROJECTS=CILAB,OCP
DCI_QUERIES=((status in ['failure', 'error']) and (tags in ['daily']))|((components.type='ocp') and (status='failure'))

# Notification Settings
ENABLE_NOTIFICATIONS=true
NOTIFICATION_CHANNEL=console
